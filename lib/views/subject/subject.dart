import 'package:flutter/material.dart';

class MySubject extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Center(child: Text("影音"))),
      body: Container(
        child: Text("影音"),
      ),
    );
  }
}
