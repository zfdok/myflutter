import 'package:flutter/material.dart';

class MyMall extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Center(child: Text("市场"))),
      body: Container(
        child: Text("Mall"),
      ),
    );
  }
}
